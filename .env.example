# MongoDB Atlas connection string
# Get this from your Atlas cluster's "Connect" button
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# Braintrust API key for experiment tracking/logging
# Get this from https://www.braintrust.dev/app/settings
# This key is used for logging eval results to your Braintrust project
BRAINTRUST_API_KEY=your_braintrust_api_key_here

# Braintrust API key for AI Proxy (LLM access)
# This can be the same as BRAINTRUST_API_KEY if your org has provider keys configured,
# or a separate key from an org that has LLM provider access
# If not set, falls back to BRAINTRUST_API_KEY
BRAINTRUST_AI_PROXY_KEY=your_ai_proxy_key_here

# Optional: Configure which models to use for code generation and scoring
# LLM calls go through Braintrust AI Proxy - configure provider API keys in your Braintrust org settings
# See supported models: https://www.braintrust.dev/docs/guides/proxy#supported-models
# Default is gpt-4o (128K context) - use a model with larger context for large documentation pages
GENERATION_MODEL=gpt-4o
SCORING_MODEL=claude-sonnet-4-5-20250929

# Optional: Maximum characters for documentation content before truncation
# Default is 300000 (~75K tokens). Adjust based on your model's context window.
# For 128K context models: 300000 leaves room for prompts
# For 200K+ context models: consider 500000-600000
# MAX_DOC_CHARS=300000
